# Metrify billing SDK
metrify-sdk>=0.2.2

# MCP server (requires Python 3.9+ / Railway environment)
mcp>=1.9.0

# Server runtime
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Upstream API clients
anthropic>=0.72.0
openai>=1.0.0
apify-client>=1.7.0
firecrawl-py>=0.0.16
# Note: assemblyai SDK requires Python 3.9+ — assemblyai_tool uses httpx REST instead

# JWT auth (RS256 — asymmetric, no shared secret)
PyJWT>=2.0.0
cryptography>=41.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
